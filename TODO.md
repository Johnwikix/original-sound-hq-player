1. [代码已修复，验收通过] 卷积校正首播前允许编辑；自定义草稿仅在本次运行内有效，首次播放及切换输出均实时生效。选择或重新应用预设后恢复设备绑定自动切换，切换绑定开关清除草稿。设备列表位置不变。
2. [代码已修复，待实机验收] 启动先加载缓存音乐库和播放状态，主界面就绪后后台扫描；刷新保留当前播放对象与队列顺序，移除队列中已确认删除的非当前歌曲；退出取消并等待扫描收尾。
3. 部分win10版本系统使用`OpenFolderAsync` 方法完全无法打开文件夹对话框。
4. 程序安全退出始终是个问题，部分用户反应程序退出时概率卡死，我自己也在debug模式下观察到退出时概率发生vs调试模式无法捕获的致命错误（可能和下一条耦合有关？）
5. 服务之间存在大量的耦合可能需要大规模重构（风险极高）
