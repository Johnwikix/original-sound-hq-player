1. [代码已修复，验收通过] 卷积校正首播前允许编辑；自定义草稿仅在本次运行内有效，首次播放及切换输出均实时生效。选择或重新应用预设后恢复设备绑定自动切换，切换绑定开关清除草稿。设备列表位置不变。
2. [代码已修复，验收通过] 启动先加载缓存音乐库和播放状态，主界面就绪后后台扫描；刷新保留当前播放对象与队列顺序，移除队列中已确认删除的非当前歌曲；退出取消并等待扫描收尾。
3. [代码已修复，用户确认验收通过] `OpenFolderAsync` 按路径启动并补 Explorer 回退；添加目录的选择器在 COM/不支持异常时回退 HWND 绑定的 WinRT 选择器，取消不重复弹窗。2026-09-20 用户确认修复。
4. [部分修复，原生致命错误待定位] 修复退出取消回调/状态订阅者异常导致整个退出中断且无法重试的问题；新增回归通过。用户报告的概率卡死/原生崩溃尚无转储与实机复现，不标记整体完成；IPC 发布与释放、在途任务及窗口生命周期按重构方案继续处理。
5. [全面审查完成，共享状态及核心用例已分阶段迁移，尚未全量完成] 保留单例公共状态入口，按领域组织共享对象；重点拆解 AppViewModel 的状态、用例、基础设施副作用，覆盖全部 ViewModel 及跨页写入口。已落地播放/设置/队列/统计/导出与生命周期修复；窗口/热键、精确队列条目身份等仍待继续迁移，库投影按需刷新已落地待实机验收。审查发现、状态归属、迁移与验收见 [重构方案](docs/ServiceRefactoringPlan.md)。
