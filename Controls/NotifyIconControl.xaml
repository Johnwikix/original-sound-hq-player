<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinUIMusicPlayer.Controls.NotifyIconControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d">

    <Grid>
        <tb:TaskbarIcon
            x:Name="NotifyIcon"
            x:FieldModifier="public"
            ContextMenuMode="SecondWindow"
            IconSource="/Assets/icon.ico"
            DoubleClickCommand="{x:Bind ShowHideWindowCommand}"
            x:Uid="AppMainTitleIcon">
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Command="{x:Bind ShowHideWindowCommand}" x:Name="Display" x:Uid="IconDisplay">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE7F4;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem x:Name="LastSong" x:Uid="IconLastSong" Click="LastSong_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE892;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem MinWidth="200" x:Name="PlayNPause" x:Uid="IconPlayNPause" Click="PlayNPause_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE768;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem x:Name="NextSong" x:Uid="IconNextSong" Click="NextSong_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE893;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem Command="{x:Bind ExitApplicationCommand}" x:Name="Exit" x:Uid="IconExit">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE89F;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
    </Grid>
</UserControl>
