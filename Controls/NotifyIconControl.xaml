<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinUIMusicPlayer.Controls.NotifyIconControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:WinUIMusicPlayer.Converters"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:PlayModeToIconConverter x:Key="PlayModeToIconConverter"/>
        <converters:PlayStatusToIconConverter x:Key="PlayStatusToIconConverter"/>
        <converters:MusicToTitleConverter x:Key="MusicToTitleConverter"/>
        <converters:VolumeToIconConverter x:Key="VolumeToIconConverter"/>
        <converters:PlayStatusToStringConverter x:Key="PlayStatusToStringConverter"/>
    </UserControl.Resources>
    <Grid>
        <tb:TaskbarIcon
            x:Name="NotifyIcon"
            x:FieldModifier="public"
            ContextMenuMode="SecondWindow"
            IconSource="/Assets/icon.ico"
            LeftClickCommand="{x:Bind ShowHideWindowCommand}"
            DataContext="{Binding}" 
            NoLeftClickDelay="True"
            ToolTipText="{Binding MusicBrowseViewModel.CurrentPlayingMusic,Converter={StaticResource MusicToTitleConverter},Mode=OneWay}"
            >
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout>                  
                    <MenuFlyoutItem Width="200" Text="{Binding MusicBrowseViewModel.CurrentPlayingMusic,Converter={StaticResource MusicToTitleConverter},Mode=OneWay}" x:Name="CurrentMusic" Command="{x:Bind ShowPlayingDetailCommand}">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE8D6;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>                    
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem x:Name="LastSong" x:Uid="IconLastSong" Click="LastSong_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE892;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem HorizontalAlignment="Left" Width="200" x:Name="PlayNPause" Text="{Binding MusicBrowseViewModel.IsPlaying,Converter={StaticResource PlayStatusToStringConverter},Mode=OneWay}" Click="PlayNPause_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon x:Name="PlayNPauseIcon" Glyph="{Binding MusicBrowseViewModel.IsPlaying,Converter={StaticResource PlayStatusToIconConverter},Mode=OneWay}"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem x:Name="NextSong" x:Uid="IconNextSong" Click="NextSong_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE893;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutSubItem x:Name="PlayModeFlyout" Text="{Binding MusicBrowseViewModel.PlayModeFlyoutText,Mode=TwoWay}" x:Uid="IconListLoopValue">
                        <MenuFlyoutSubItem.Icon>
                            <FontIcon x:Name="PlayModeFlyoutIcon" Glyph="{Binding MusicBrowseViewModel.CurrentPlayMode,Converter={StaticResource PlayModeToIconConverter},Mode=TwoWay}"></FontIcon>
                        </MenuFlyoutSubItem.Icon>
                        <ToggleMenuFlyoutItem x:Name="IconRepeatAll" x:Uid="IconListLoopValue" Click="PlayMode_Click">
                            <ToggleMenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE8EE;"></FontIcon>
                            </ToggleMenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                        <ToggleMenuFlyoutItem x:Name="IconRepeatOne" x:Uid="IconSingleTuneCirculationValue" Click="PlayMode_Click">
                            <ToggleMenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE8ED;"></FontIcon>
                            </ToggleMenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                        <ToggleMenuFlyoutItem x:Name="IconRepeatOff" x:Uid="IconSinglePlaybackValue" Click="PlayMode_Click">
                            <ToggleMenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xF5E7;"></FontIcon>
                            </ToggleMenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                        <ToggleMenuFlyoutItem x:Name="IconShuffle" x:Uid="IconRandomLoopValue" Click="PlayMode_Click">
                            <ToggleMenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE8B1;"></FontIcon>
                            </ToggleMenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                    </MenuFlyoutSubItem>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutSubItem x:Name="Volume" x:Uid="IconVolume">
                        <MenuFlyoutSubItem.Icon>
                            <FontIcon Glyph="{Binding MusicBrowseViewModel.Volume,Converter={StaticResource VolumeToIconConverter},Mode=OneWay}"/>
                        </MenuFlyoutSubItem.Icon>
                        <MenuFlyoutItem x:Name="VolumeUp" Text="+10" Click="VolumeUp_Click"/>
                        <MenuFlyoutItem x:Name="VolumeDown" Text="-10" Click="VolumeDown_Click"/>
                    </MenuFlyoutSubItem>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem x:Name="Settings" x:Uid="IconSettings" Click="Settings_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE713;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem Command="{x:Bind ExitApplicationCommand}" x:Name="Exit" x:Uid="IconExit">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE89F;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
    </Grid>
</UserControl>
