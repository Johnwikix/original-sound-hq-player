<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIMusicPlayer.View.SongListPage"
    NavigationCacheMode="Enabled"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.View"
    xmlns:model="using:WinUIMusicPlayer.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    IsTabStop="True"
    TabIndex="0"
    xmlns:converters="using:WinUIMusicPlayer.Converters">
    <Page.Resources>
        <converters:TimeSpanToStringConverter x:Key="TimeSpanToStringConverter"/>
        <converters:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
        <Style x:Key="ListViewItemStyle" TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
            <Setter Property="ContextFlyout">
                <Setter.Value>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="播放" Click="PlayMenuItem_Click"/>
                        <MenuFlyoutSubItem Text="更多操作">
                            <MenuFlyoutItem Text="添加到播放列表" Click="AddToPlaylistMenuItem_Click"/>
                            <MenuFlyoutItem Text="设为最爱" Click="SetAsFavoriteMenuItem_Click"/>
                        </MenuFlyoutSubItem>
                        <MenuFlyoutItem Text="打开文件位置" Click="OpenInExplorer_Click"/>
                        <MenuFlyoutItem Text="删除" Click="DeleteMenuItem_Click"/>
                    </MenuFlyout>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <ListView x:Name="MusicListView" 
              DoubleTapped="MusicListView_DoubleTapped" 
              ItemContainerStyle="{StaticResource ListViewItemStyle}" 
              RightTapped="MusicListView_RightTapped">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="model:Music">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="4*"/>
                        <ColumnDefinition Width="0.5*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{x:Bind Title}" Grid.Column="0" Margin="5" VerticalAlignment="Center" />
                    <TextBlock Text="{x:Bind Author}" Grid.Column="1" Margin="5" VerticalAlignment="Center" />
                    <TextBlock Text="{x:Bind Album}" Grid.Column="2" Margin="5" VerticalAlignment="Center" />
                    <TextBlock Text="{x:Bind Duration, Converter={StaticResource TimeSpanToStringConverter}}" Grid.Column="3" Margin="5" VerticalAlignment="Center"  />
                    <Button x:Name="RemoveMusicButton"  Grid.Column="4" Click="RemoveMusicButton_Click" Tag="{x:Bind Id}" VerticalAlignment="Center" Margin="5" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" >
                        <FontIcon Glyph="&#xecc9;"/>
                    </Button>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page> 
