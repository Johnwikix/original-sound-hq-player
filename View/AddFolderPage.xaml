<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIMusicPlayer.View.AddFolderPage"
    NavigationCacheMode="Enabled"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.View"
    xmlns:model="using:WinUIMusicPlayer.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid>
        <!-- Loading 界面 -->
        <Grid x:Name="LoadingGrid" Visibility="Collapsed">
            <ProgressRing IsActive="True" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="100"/>
            <TextBlock Text="正在处理数据，请稍候..." HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,180,0,0"/>
        </Grid>
        <Grid 
            Margin="0 38 0 0" 
            x:Name="AddFolderGrid"
            AllowDrop="True"
            DragOver="Grid_DragOver"
            Drop="Grid_Drop"
            >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button x:Name="AddFolder" Click="AddFolderButton_Click" Margin="25 10 10 10"  BorderThickness="0" BorderBrush="Transparent" Background="Transparent" >
                <FontIcon Glyph="&#xe8f4;"/>
            </Button>
            <ListView x:Name="FolderListView" ItemsSource="{Binding ViewModel.FolderList,Mode=TwoWay}" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:Folder">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="OpenFolderButton" Tag="{x:Bind Id}" Click="OpenFolderButton_Click" Grid.Column="0" Margin="10" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" >
                                <FontIcon Glyph="&#xe838;"/>
                            </Button>
                            <StackPanel Orientation="Vertical" Grid.Column="1" Margin="10">
                                <TextBlock Text="{x:Bind Name}" FontSize="16"/>
                                <TextBlock Text="{x:Bind Path}" FontSize="12"/>
                                <!--<TextBlock Text="{x:Bind Type}" FontSize="12"/>-->
                            </StackPanel>
                            <Button x:Name="RescanFolderButton" Tag="{x:Bind Id}" Click="RescanFolderButton_Click" Grid.Column="2" Margin="10" BorderThickness="0" BorderBrush="Transparent" Background="Transparent" >
                                <FontIcon Glyph="&#xe72c;"/>
                            </Button>
                            <Button x:Name="RemoveFolderButton" Grid.Column="3" Click="RemoveFolderButton_Click" Tag="{x:Bind Id}" Margin="10" BorderThickness="0" BorderBrush="Transparent" Background="Transparent">
                                <FontIcon Glyph="&#xecc9;"/>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
