<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIMusicPlayer.View.FolderBrowsePage"
    NavigationCacheMode="Enabled"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:WinUIMusicPlayer.Converters"
    mc:Ignorable="d"    >
    <Page.Resources>
        <converters:IsExistOnDeviceConverter x:Key="IsExistOnDeviceConverter"/>
    </Page.Resources>
    <Grid Margin="10">
        <GridView x:Name="FolderGridView"                       
                  IsItemClickEnabled="True"
                  ItemClick="FolderGridView_ItemClick"
                  IsRightTapEnabled="True"
                  RightTapped="Folder_RightTapped"
                  >
            <!--<GridView.ItemContainerTransitions>
                <TransitionCollection>
                    <AddDeleteThemeTransition />
                    <EntranceThemeTransition />
                </TransitionCollection>
            </GridView.ItemContainerTransitions>-->
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Visibility="Collapsed" Grid.Row="0">
                            <ProgressRing IsActive="True" HorizontalAlignment="Center" VerticalAlignment="Center" Width="50" Height="50"/>
                        </Grid>
                        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5">
                            <Grid Width="150" Height="150">
                                <!-- 图片在下层 -->
                                <FontIcon Glyph="&#xf12b;" FontSize="100" Width="150" Height="150"/>
                                <!-- 图标位于右下角 -->
                                <FontIcon 
                                    x:Name="IsExistOnDeviceIcon" 
                                    FontSize="16" 
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    Margin="0,0,5,5"
                                    Glyph="{Binding IsExistOnDevice, Mode=OneWay, Converter={StaticResource IsExistOnDeviceConverter}}"/>
                            </Grid>
                            
                            <TextBlock 
                                Height="60" 
                                HorizontalAlignment="Center" 
                                Width="150" 
                                TextWrapping="Wrap" 
                                TextTrimming="CharacterEllipsis"
                                Text="{Binding LastLevelFolderPath}" 
                                TextAlignment="Center" 
                                Margin="0"
                                VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
