<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinUIMusicPlayer.View.FolderBrowsePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:WinUIMusicPlayer.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="using:WinUIMusicPlayer.Extensions"
    xmlns:local="using:WinUIMusicPlayer.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:WinUIMusicPlayer.Model"
    NavigationCacheMode="Enabled"
    mc:Ignorable="d">
    <Page.Resources>
        <CollectionViewSource
            x:Name="FolderCollectionSource"
            IsSourceGrouped="True"
            ItemsPath="Items"
            Source="{x:Bind ViewModel.AppViewModel.FolderPageSource}" />
    </Page.Resources>
    <Grid Margin="0">
        <SemanticZoom>
            <!--  详细视图 (ZoomedInView)  -->
            <SemanticZoom.ZoomedInView>
                <GridView
                    x:Name="FolderGridView"
                    Margin="10,0,0,0"
                    ContainerContentChanging="FolderGridView_ContainerContentChanging"
                    IsItemClickEnabled="True"
                    IsRightTapEnabled="True"
                    ItemClick="FolderGridView_ItemClick"
                    ItemsSource="{x:Bind FolderCollectionSource.View, Mode=OneWay}"
                    RightTapped="Folder_RightTapped"
                    SelectionMode="None"
                    VirtualizingStackPanel.VirtualizationMode="Standard">
                    <GridView.ItemContainerStyle>
                        <Style BasedOn="{StaticResource DefaultGridViewItemStyle}" TargetType="GridViewItem">
                            <Setter Property="ContextFlyout">
                                <Setter.Value>
                                    <MenuFlyout extensions:MenuFlyoutExtensions.ItemsSource="{x:Bind ViewModel.FolderMenuOptions, Mode=OneWay}" />
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GridView.ItemContainerStyle>
                    <GridView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate x:DataType="model:GenericGroup">
                                    <TextBlock
                                        Margin="10,0,5,5"
                                        FontSize="28"
                                        FontWeight="SemiBold"
                                        Text="{x:Bind Key}" />
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                            <GroupStyle.HeaderContainerStyle>
                                <StaticResource ResourceKey="NoBorderGridViewHeaderItemStyle" />
                            </GroupStyle.HeaderContainerStyle>
                        </GroupStyle>
                    </GridView.GroupStyle>
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="model:Music">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Row="0" Visibility="Collapsed">
                                    <ProgressRing
                                        Width="50"
                                        Height="50"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        IsActive="True" />
                                </Grid>
                                <StackPanel
                                    Grid.Row="0"
                                    Margin="5"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                    <Grid Width="150" Height="150">
                                        <!--  图片在下层  -->
                                        <Border
                                            Grid.Row="0"
                                            Width="150"
                                            Height="150"
                                            Margin="0,0,0,0"
                                            Padding="0"
                                            CornerRadius="10">
                                            <Grid>
                                                <Image
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Canvas.ZIndex="1"
                                                    Source="{x:Bind Converter={StaticResource AlbumArtConverter}}"
                                                    Stretch="UniformToFill">
                                                    <Image.Transitions>
                                                        <TransitionCollection>
                                                            <ContentThemeTransition />
                                                        </TransitionCollection>
                                                    </Image.Transitions>
                                                </Image>
                                                <Grid
                                                    Width="150"
                                                    Height="150"
                                                    Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
                                                    Canvas.ZIndex="0">
                                                    <FontIcon
                                                        Canvas.ZIndex="0"
                                                        FontSize="50"
                                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                        Glyph="&#xf12b;" />
                                                </Grid>
                                            </Grid>
                                        </Border>
                                        <!--  图标位于右下角  -->
                                        <FontIcon
                                            x:Name="IsExistOnDeviceIcon"
                                            Margin="0,0,5,5"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            FontSize="16"
                                            Glyph="{x:Bind IsExistOnDevice, Mode=OneWay, Converter={StaticResource IsExistOnDeviceConverter}}" />
                                    </Grid>

                                    <TextBlock
                                        Width="150"
                                        Height="60"
                                        Margin="0,5,0,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Text="{x:Bind LastLevelFolderPath}"
                                        TextAlignment="Center"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </SemanticZoom.ZoomedInView>

            <!--  概览视图 (ZoomedOutView)  -->
            <SemanticZoom.ZoomedOutView>
                <GridView
                    x:Name="ZoomedOutGridView"
                    MaxWidth="600"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    IsItemClickEnabled="True"
                    ItemsSource="{x:Bind FolderCollectionSource.View.CollectionGroups, Mode=OneWay}"
                    ScrollViewer.IsHorizontalScrollChainingEnabled="False"
                    ScrollViewer.IsVerticalScrollChainingEnabled="False"
                    ScrollViewer.ZoomMode="Disabled">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="ICollectionViewGroup">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="32"
                                Text="{x:Bind Group.(model:GenericGroup.Key), Mode=OneWay}" />
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </SemanticZoom.ZoomedOutView>
        </SemanticZoom>
    </Grid>
</Page>
