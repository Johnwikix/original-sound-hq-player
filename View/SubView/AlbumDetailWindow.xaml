<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUIMusicPlayer.View.SubView.AlbumDetailWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.View.SubView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AlbumDetailWindow">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop/>
    </Window.SystemBackdrop>
    <Grid>
        <Grid
        x:Name="AlbumDetailTitleBar"
        Height="44"
        Margin="0"
        VerticalAlignment="Top"
        Padding="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
            Margin="18,0,0,0"
            Grid.Column="1"
            FontSize="16"
            VerticalAlignment="Center"
            Style="{StaticResource CaptionTextBlockStyle}"
            Text="专辑详情"/>
        </Grid>
        <Grid x:Name="LoadingGrid" Visibility="Collapsed">
            <ProgressRing IsActive="True" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="100"/>
            <TextBlock Text="正在处理保存，请稍候..." HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,180,0,0"/>
        </Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <Grid Margin="0 50 0 10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Image x:Name="AlbumCoverImage" Grid.Row="1" Stretch="UniformToFill" HorizontalAlignment="Stretch" VerticalAlignment="Top"></Image>
                    <Grid Grid.Row="0" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="1" >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left">
                                <Button Click="GetImageFromNet_Click" Margin="5" HorizontalAlignment="Center">
                                    <Button.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <FontIcon Glyph="&#xebd3;" FontSize="16"/>
                                        </StackPanel>
                                    </Button.Content>
                                    <ToolTipService.ToolTip>
                                        <ToolTip>
                                            在线匹配封面
                                        </ToolTip>
                                    </ToolTipService.ToolTip>
                                </Button>
                                <Button Click="SaveImageButton_Click" Margin="5" HorizontalAlignment="Stretch">
                                    <Button.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <FontIcon Glyph="&#xe78c;" FontSize="16"/>
                                        </StackPanel>
                                    </Button.Content>
                                    <ToolTipService.ToolTip>
                                        <ToolTip>
                                            保存到本地
                                        </ToolTip>
                                    </ToolTipService.ToolTip>
                                </Button>
                                <Button Click="SelectCoverImageButton_Click" Margin="5" HorizontalAlignment="Stretch">
                                    <Button.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <FontIcon Glyph="&#xe70f;" FontSize="16"/>
                                        </StackPanel>
                                    </Button.Content>
                                    <ToolTipService.ToolTip>
                                        <ToolTip>
                                            编辑封面
                                        </ToolTip>
                                    </ToolTipService.ToolTip>
                                </Button>
                            </StackPanel>
                            <TextBlock Text="标题：" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5" Grid.Row="0" Grid.Column="1"/>
                            <TextBox x:Name="AlbumTextBlock"  BorderThickness="0" TextWrapping="Wrap" HorizontalAlignment="Stretch" Margin="5" Grid.Row="0"  Grid.Column="2"/>
                            <TextBlock Text="年份：" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5" Grid.Row="0"  Grid.Column="3"/>
                            <TextBox x:Name="YearTextBlock" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="5" Grid.Row="0"  Grid.Column="4"/>
                        </Grid>
                    </Grid>
                </Grid>
            </ScrollViewer>
            <StackPanel
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Grid.Row="1"    
                    >
                <Button
                    Content="保存"
                    Margin="5">
                    <Button.Flyout>
                        <Flyout x:Name="ConfirmFlyout">
                            <StackPanel>
                                <TextBlock Style="{ThemeResource BaseTextBlockStyle}" Text="是否确定保存元信息至专辑内所有音乐" Margin="0,0,0,12" />
                                <StackPanel Orientation="Horizontal">
                                    <Button Click="ConfirmButton_Click" Content="确认" Margin="0 0 10 0"/>
                                    <Button Content="取消"  Click="CloseFlyoutButton_Click" Margin="0 0 10 0"/>
                                </StackPanel>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>
                <Button
                    Content="关闭"
                    Click="CloseButton_Click"
                    Margin="5"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
