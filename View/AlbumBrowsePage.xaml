<Page
    x:Class="WinUIMusicPlayer.View.AlbumBrowsePage"
    NavigationCacheMode="Enabled"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.View"
    xmlns:model="using:WinUIMusicPlayer.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Margin="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="AlbumItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Button Width="150" Height="150" Margin="0" BorderThickness="0" BorderBrush="Transparent" Background="Transparent"
                                    Click="Album_Click" Tag="{Binding}">
                                <Image  x:Name="AlbumCoverImage" Width="130" Height="130" Stretch="Uniform" Source="{Binding Cover, FallbackValue='ms-appx:///Assets/Album.png'}"/>
                            </Button>
                            <TextBlock 
                                Height="60" 
                                HorizontalAlignment="Center" 
                                Width="150" 
                                TextWrapping="Wrap" 
                                TextTrimming="CharacterEllipsis"
                                Text="{Binding Album}" 
                                TextAlignment="Center" 
                                Margin="0"
                                VerticalAlignment="Center"
                            />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>