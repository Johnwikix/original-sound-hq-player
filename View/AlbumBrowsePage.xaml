<Page
    x:Class="WinUIMusicPlayer.View.AlbumBrowsePage"
    NavigationCacheMode="Enabled"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.View"
    xmlns:model="using:WinUIMusicPlayer.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Margin="10">
        <ItemsControl x:Name="AlbumItemsControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Width="200" Height="200" Margin="10" BorderThickness="0" BorderBrush="Transparent" Background="Transparent"
                            Click="Album_Click" Tag="{Binding}">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Image Width="150" Height="150" Stretch="Fill">
                                <Image.Source>
                                    <BitmapImage UriSource="ms-appx:///Assets/Album.png"/>
                                </Image.Source>
                            </Image>
                            <TextBlock Text="{Binding Album}" TextAlignment="Center" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>