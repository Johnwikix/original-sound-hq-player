<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIMusicPlayer.View.MusicBrowsePage"
    NavigationCacheMode="Enabled"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIMusicPlayer.View"
    xmlns:model="using:WinUIMusicPlayer.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView x:Name="MusicListView" DoubleTapped="MusicListView_DoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Music">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind Title}" Margin="10" />
                        <TextBlock Text="{x:Bind Author}" Margin="10" />
                        <Button Content="移除" Click="RemoveMusicButton_Click" Tag="{x:Bind Id}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button Content="播放" Click="PlayButton_Click"/>
            <Image x:Name="AlbumCoverImage" Width="50" Height="50" Margin="10"/>
            <StackPanel Orientation="Vertical">
                <TextBlock x:Name="MusicTitleTextBlock" FontSize="16" Margin="10"/>
                <TextBlock x:Name="MusicAuthorTextBlock" FontSize="12" Margin="10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
